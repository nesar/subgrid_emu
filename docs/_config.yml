# GitHub Pages Configuration for subgrid_emu

# Theme (not used since we have custom HTML/CSS)
theme: null

# Include files
include:
  - index.html
  - styles.css
  - script.js

# Exclude files
exclude:
  - README.md
