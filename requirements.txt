# Core dependencies
numpy>=1.21.0
# scipy pinned to <1.12 because SEPIA uses scipy.linalg.solve(sym_pos=True) which was removed in 1.12.0
scipy>=1.7.0,<1.12
sepia @ git+https://github.com/lanl/SEPIA.git

# Plotting dependencies
matplotlib>=3.5.0
pandas>=1.3.0

# Development dependencies (optional)
pytest>=6.0
pytest-cov>=2.0
